events {
    worker_connections 1024;
}

http {
    # Upstream a FastAPI Dashboard
    upstream chocolate_dashboard {
        server chocolate_factory_brain:8000;
    }

    # Redirect HTTP to HTTPS
    server {
        listen 80;
        server_name ${TAILSCALE_DOMAIN};
        return 301 https://$server_name$request_uri;
    }

    # HTTPS Server - Solo Dashboard TFM Chocolate Factory
    server {
        listen 443 ssl http2;
        server_name ${TAILSCALE_DOMAIN};

        # SSL Configuration
        ssl_certificate /etc/nginx/ssl/${TAILSCALE_DOMAIN}.crt;
        ssl_certificate_key /etc/nginx/ssl/${TAILSCALE_DOMAIN}.key;
        
        # SSL Settings Optimized
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        # Security Headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Frame-Options DENY;
        add_header X-Content-Type-Options nosniff;
        add_header X-XSS-Protection "1; mode=block";

        # REDIRIGIR ROOT A DASHBOARD
        location = / {
            return 302 /dashboard;
        }

        # SOLO DASHBOARD - Bloquear otras rutas
        location /dashboard {
            proxy_pass http://chocolate_dashboard;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Headers para dashboard dinámico
            proxy_set_header Connection "upgrade";
            proxy_set_header Upgrade $http_upgrade;
            proxy_cache_bypass $http_upgrade;
        }

        # Permitir recursos estáticos del dashboard
        location ~ ^/(static|css|js|images|fonts)/ {
            proxy_pass http://chocolate_dashboard;
            add_header Cache-Control "public, max-age=86400";
        }

        # API endpoints necesarios para el dashboard
        location /dashboard/complete {
            proxy_pass http://chocolate_dashboard;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # BLOQUEAR TODO LO DEMÁS - Seguridad
        location ~ ^/(docs|redoc|openapi\.json|predict|mlflow|ree|aemet|weather|init|gaps|models|scheduler|influxdb) {
            return 403;
        }

        # Bloquear APIs administrativas
        location ~ ^/(admin|api/admin) {
            return 403;
        }

        # Mensaje por defecto para rutas no permitidas
        location / {
            return 403 '<!DOCTYPE html>
<html>
<head>
    <title>TFM Chocolate Factory - Acceso Restringido</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; margin-top: 100px; }
        .container { max-width: 600px; margin: 0 auto; }
        .chocolate { font-size: 4rem; margin-bottom: 1rem; }
        h1 { color: #8B4513; }
        p { color: #555; font-size: 1.1rem; }
        .link { color: #0066cc; text-decoration: none; font-weight: bold; }
        .link:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <div class="container">
        <div class="chocolate">🍫</div>
        <h1>TFM Chocolate Factory</h1>
        <h2>Dashboard Linares, Andalucía</h2>
        <p>Esta zona está restringida para seguridad.</p>
        <p><a href="/dashboard" class="link">👉 Ir al Dashboard</a></p>
        <br>
        <small>Acceso solo a través de red Tailscale | SSL/TLS habilitado</small>
    </div>
</body>
</html>';
        }
    }
}