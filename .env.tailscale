# =============================================================================
# TFM CHOCOLATE FACTORY - TAILSCALE SIDECAR CONFIGURATION
# =============================================================================
# Copiar este archivo como .env.tailscale y completar con tu auth key
# =============================================================================

# REQUIRED: Tailscale Auth Key
# Genera una auth key en: https://login.tailscale.com/admin/settings/keys
# Configura como "Reusable" y con tiempo de expiración adecuado
TAILSCALE_AUTHKEY=***REMOVED***

# OPCIONAL: Configuración adicional
TAILSCALE_HOSTNAME=chocolate-factory

# =============================================================================
# INSTRUCCIONES DE USO:
# =============================================================================
# 1. Copiar este archivo: cp .env.tailscale.example .env.tailscale
# 2. Generar auth key en el panel de Tailscale
# 3. Pegar la auth key arriba
# 4. Ejecutar: docker compose --env-file .env.tailscale up chocolate-factory
# =============================================================================