# =============================================================================
# CHOCOLATE FACTORY - GITIGNORE SECURITY
# =============================================================================
# CRÍTICO: Protege información sensible antes de subir a GitHub
# =============================================================================

# -----------------------------------------------------------------------------
# 🚨 ARCHIVOS CON SECRETOS Y TOKENS (CRÍTICO - NO SUBIR)
# -----------------------------------------------------------------------------
.env*
!.env.example
*.key
*.pem  
*.crt
*.cert
*.p12
*.pfx

# Tailscale auth keys
.env.tailscale*
!.env.tailscale.example
tskey-*
tailscale_*.key

# API tokens y secretos
*secret*
!secrets.enc.yaml  # SOPS encrypted file - safe to commit
*token*
*api-key*
*password*

# -----------------------------------------------------------------------------
# 🔐 SSL/TLS CERTIFICATES (CRÍTICO)
# -----------------------------------------------------------------------------
ssl/
certs/
certificates/
*.ts.net.*

# -----------------------------------------------------------------------------
# 💾 DOCKER DATA VOLUMES (CRÍTICO - CONTIENEN DATOS SENSIBLES)
# -----------------------------------------------------------------------------
docker/services/*/data/
docker/services/influxdb/data/
docker/services/postgres/data/
docker/services/mlflow/artifacts/
docker/volumes/

# -----------------------------------------------------------------------------
# 📊 DATOS SENSIBLES Y MODELOS ML
# -----------------------------------------------------------------------------
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
logs/
*.log
*.db
*.sqlite
*.sqlite3

# Modelos ML entrenados (contienen datos sensibles del entrenamiento)
models/
!models/.gitkeep
*.pkl
*.joblib
*.model

# -----------------------------------------------------------------------------
# 🐍 PYTHON
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv/
.env/

# Pytest
.pytest_cache/
.tox/
.coverage
htmlcov/

# -----------------------------------------------------------------------------
# 🔨 DEVELOPMENT TOOLS
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# -----------------------------------------------------------------------------
# 📦 DOCKER & DEPLOYMENT
# -----------------------------------------------------------------------------
.docker/
docker-compose.override.yml
docker-compose.prod.yml

# Docker secrets (DEPRECATED - Eliminados, ahora usa SOPS)
docker/secrets/*
!docker/secrets/.gitkeep
!docker/secrets/README.md

# SOPS - Age encryption keys (CRÍTICO - NO SUBIR)
.sops/age-key.txt
.sops/secrets.yaml

# Local .env file (decrypted from secrets.enc.yaml)
.env

# Scripts de empaquetado
package-for-windows.sh
chocolate-factory-package/

# -----------------------------------------------------------------------------
# 📋 TEMPORARY FILES
# -----------------------------------------------------------------------------
*.tmp
*.temp
*.bak
*.backup
*.cacheage/
