# =============================================================================
# TFM CHOCOLATE FACTORY - TAILSCALE SIDECAR CONFIGURATION EXAMPLE
# =============================================================================
# INSTRUCCIONES:
# 1. Copiar este archivo: cp .env.tailscale.example .env.tailscale
# 2. Generar auth key en: https://login.tailscale.com/admin/settings/keys
# 3. Completar con tu auth key real
# 4. NUNCA subir .env.tailscale a Git
# =============================================================================

# REQUIRED: Tailscale Auth Key
# Genera una auth key en: https://login.tailscale.com/admin/settings/keys
# Configura como "Reusable" y con tiempo de expiración adecuado
TAILSCALE_AUTHKEY=tskey-auth-<your-tailscale-key-example>

# OPCIONAL: Configuración adicional
TAILSCALE_HOSTNAME=chocolate-factory

# =============================================================================
# INSTRUCCIONES DE USO:
# =============================================================================
# 1. Generar auth key en el panel de Tailscale
# 2. Pegar la auth key arriba
# 3. Ejecutar: docker compose --env-file .env.tailscale up chocolate-factory
# =============================================================================

# NOTA: Este archivo es solo un ejemplo. No subir a Git con datos reales.